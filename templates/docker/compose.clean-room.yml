services:
  node22:
    image: clean-room-skill/node22:local
    build:
      context: profiles
      dockerfile: node22.Dockerfile
  python312:
    image: clean-room-skill/python312:local
    build:
      context: profiles
      dockerfile: python312.Dockerfile
  go126:
    image: clean-room-skill/go126:local
    build:
      context: profiles
      dockerfile: go126.Dockerfile
  rust-stable:
    image: clean-room-skill/rust-stable:local
    build:
      context: profiles
      dockerfile: rust-stable.Dockerfile
